services:
  backend:
    image: advanced-rag-backend:v1
    ports:
      - "8000:8000"
    environment:
      - GROQ_API_KEY=${GROQ_API_KEY}

  frontend:
    image: advanced-rag-frontend:v1
    ports:
      - "8501:8501"
    environment:
      - BACKEND_URL=http://backend:8000